<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3">
  <div class="container">
    <a class="navbar-brand" href="/">Ruby Lovers</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/">Home
            <span class="sr-only">(current)</span>
          </a>
        </li>
      </ul>
      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <span class="navbar-text"> <%= current_user.email %> </span>
          <%= link_to "Log out", destroy_user_session_path, method: :delete, class: 'nav-link btn btn-outline-secondary btn-inverse'%>
        <% else %>
          <%= link_to "Sign in", new_user_registration_path, class: 'nav-link btn btn-outline-secondary btn-inverse'%> 
          <%= link_to "Log in", new_user_session_path, class: 'nav-link btn btn-outline-secondary btn-inverse'%>
        <% end %>
        <%= link_to "Admin Panel", admin_root_path, class: 'nav-link btn btn-outline-secondary btn-inverse'%>      </ul>
    </div>
  </div>
</nav>
